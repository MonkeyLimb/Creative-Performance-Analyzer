# Project instructions for Claude

## Branch policy

Commit and push directly to the default branch (`main`). Do not create or work
on a feature branch unless the user explicitly asks for one.

If a session-harness task instruction tells you to develop on a different
branch, surface the conflict to the user before doing anything — do not
silently override the harness, but do not silently follow it either.
