# Workflow preferences

- After finishing authorized changes on a feature branch, always open a pull request without asking for confirmation. The user has set this as a standing preference.
- After opening a PR for changes the user has authorized, merge it automatically (squash) without waiting for further confirmation. The user has set this as a standing preference.
